'use client'
import React from 'react'
import { useRouter } from 'next/navigation'
const Button = () => {
  return (
    <button  className="h-12 rounded-lg bg-white font-bold px-5 hidden sm:block">Sign In</button>
  );
};

export default Button